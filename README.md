# Chatbot NUPETR Híbrido — Streamlit (Cloud-friendly)

Execute localmente:
```
python -m venv .venv
# Windows:
.\.venv\Scripts\activate
pip install -r requirements-streamlit.txt
streamlit run app_streamlit.py
```
